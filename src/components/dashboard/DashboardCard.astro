---
interface Props {
  class?: string;
  id?: string;
  muted?: boolean;
}

const { class: className, id, muted = false } = Astro.props;
---
<section
  id={id}
  class:list={[
    muted ? "card-muted card-compact" : "card card-compact",
    className,
  ]}
>
  <slot />
</section>
